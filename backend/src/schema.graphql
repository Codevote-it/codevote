scalar Date

type Query {
    codevote(id: ID!): Codevote
    """ Login via /auth/github/login and use the retrieved token as Bearer token"""
    me: User
}

type Mutation {
    createCodevote: Codevote
}

type Codevote {
    id: ID!
    createdAt: Date!
    snippet1: Snippet!
    snippet2: Snippet!
    creator: User!
}

type Snippet {
    title: String!
    content: String!
}

type User {
    id: ID!
    displayName: String!
    username: String!
    profileImageUrl: String
}